import { css } from 'lit-element'

export const buttonReset = css`
  button {
    padding: 0;
    margin: 0;
    border: none;
    background: transparent;
    outline: none;
    cursor: pointer;
    font: inherit;
    color: inherit;
  }
`
